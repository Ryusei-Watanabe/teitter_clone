<%= render 'header' %>
<div class=body>
<h3>以下の内容で、送信する。</h3>
<%= form_with(model: @kadai, local: true, url: kadais_path) do |form| %>
  <%= form.hidden_field :content %>
  <p>本文:<%= @kadai.content %></p>
  <%= form.submit  "ツイート" %>
  <%= form.submit "戻る", name:'back' %>
</div>
  <%= render 'footer' %>
<% end %>
